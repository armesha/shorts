# Audio avatars

Local runtime avatar binaries for the `/audio` director.

Do not commit `.vrm`, `.glb`, or preview image files from this directory. They are served through protected admin API routes and stay local to the production host.
