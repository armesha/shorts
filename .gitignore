# deps & build
node_modules/
dist/
*.log

# stale Python experiments (this is a Node/TS project — kept out of the repo)
process_anecdotes*.py

# runtime data (db, rendered images/videos). The anecdote PACKS are shipped on purpose.
data/output/
data/packs/
data/*.db
data/*.db-*
data/anecdotes/_*.json
data/anecdotes-de/_*.json
data/anecdotes-it/_*.json
data/anecdotes-fr/_*.json
data/tips/_*.json

# huge source corpora — not needed at runtime (packs are pre-built & shipped)
Русские анекдоты/
corpora/

# pre-built video packs (decks "fact-en", "quotes-de", "space") — big mp4 pool + generated manifests, kept local
assets/fact-videos/
data/fact-videos/
data/quotes-de/
data/space/
data/funny-animals/

# stray generated preview images (not source)
images/

# scratch / temp working dir — not source
temp/
.playwright-mcp/

# strike-audit artifacts & deck backups — contain the offending text, NEVER commit
strike-audit-*.md
strike-audit-*.json
*.bak-strike-*

# env & secrets — NEVER commit
.env
.env.local
.env.*.local

# Google OAuth client secret — NEVER commit, NEVER read
client_secret_*.json
*.apps.googleusercontent.com.json

# DB backups (e.g. data/app.db.bak-*) — NEVER commit
data/*.db.bak*

# python bytecode cache
__pycache__/

# scratch screenshots at repo root — agent working files, not source
/image.png
/gen-queue-toast.png
